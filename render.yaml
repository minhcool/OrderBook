services:
  - type: web
    name: orderbook-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: orderbook-db
          property: connectionString
      - key: CLERK_JWT_KEY
        sync: false
      - key: ORDERBOOK_ADMIN_TOKEN
        sync: false
      - key: ORDERBOOK_BOT_KEYS
        sync: false
      - key: ORDERBOOK_RATE_LIMIT_PER_MINUTE
        value: "240"
      - key: ORDERBOOK_MUTATION_RATE_LIMIT_PER_MINUTE
        value: "80"
      - key: ORDERBOOK_LIVE_WAIT_MS
        value: "2500"
      - key: ORDERBOOK_START_DELAY_SECONDS
        value: "90"
      - key: ORDERBOOK_GAME_DURATION_SECONDS
        value: "600"
      - key: ORDERBOOK_REJOIN_COOLDOWN_SECONDS
        value: "60"
      - key: ORDERBOOK_STARTING_CASH
        value: "100000"
      - key: ORDERBOOK_CHECKPOINT_EVERY_EVENTS
        value: "100"

databases:
  - name: orderbook-db
    plan: free
